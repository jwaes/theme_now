import logging
from odoo.upgrade import util


_logger = logging.getLogger(__name__)


# def migrate(cr, version):


    # util.remove_view(cr, xml_id='theme_now.option_header_brand_logo')
    # util.remove_view(cr, xml_id='theme_now.template_header_default')
    # util.remove_view(cr, xml_id='theme_now.navbar')
    # util.remove_view(cr, xml_id='theme_now.submenu')
    # util.remove_view(cr, xml_id='theme_now.template_header_default_cart_disable')                
    # util.remove_view(cr, xml_id='theme_now.template_header_default_wish_disable')
    # util.remove_view(cr, xml_id='theme_now.website_sale_product')
    # util.remove_view(cr, xml_id='theme_now.product')     
    # util.remove_view(cr, xml_id='theme_now.template_header_default_cart')
    # util.remove_view(cr, xml_id='theme_now.template_header_default')                
    # util.remove_view(cr, xml_id='theme_now.template_header_now')
    # util.remove_view(cr, xml_id='theme_now.header_cart_link')
    # util.remove_view(cr, xml_id='theme_now.template_footer_now2')       
    

